// Custom themes and colors will be implemented in this file for global app use

import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    spacing: 4
});

export default theme;